변수에는 다양한 타입이 있다.

숫자형, 문자열, 참과 거짓,

문자열에서만 " < 따옴표를 사용한다.

위 타입들이 서로 섞인 타입도 있다.(자료형)

ex) 배열
    let fruits = [
        'banana',
        'apple'
    ];

    객체
    let person = {
        name: 'steve',
        age: 32,
        is_student = True
    };

undefined 과 함수도 타입이다.