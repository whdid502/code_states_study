반복문을 이용해 배열의 요소를 한번씩 출력하기

ex) let my_number = [73,98,86,61]

    for (let i = 0; i < my_number.length; i++){
        console.log(my_number[i])
    }

반복문의 모든 요소 더하기

ex) let sum = 0;

    for (let n = 0 ; n < my_number.length ; n++){
        sum = sum + my_number[i]
    }
    console.log(sum)