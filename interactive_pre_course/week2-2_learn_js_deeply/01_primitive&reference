number,string,boolean같은 고정된 저장공간을 사용하는 것을 원시타입(primitive)라고한다.

array,object같은 유동된 저장공간을 사용하는 것을 주소타입(reference)이라고 한다.

원시타입에는 number, boolean, null, undefined, string, biginit, symbol, (null)이 해당된다.

원시타입은 stack에 할당된다.

참조타입에는 array, object, function이 해당된다.(원시타입이 아닌 모든것이 참조타입이다.)

참조타입은 heap에 할당된다.

원시타입 데이터는 각 변수간에 원시타입 데이터를 복사할 경우에 데이터값이 복사되므로 기존의 데이터에 영향이 가지 않는다.

참조타입 데이터는 주소를 복사하기에 복사한데이터에서 주소의 내용을 변경한다면 기존의 데이터 또한 변경된다.

원시타입은 하나의 의미를 가지는 데이터에는 변함이 없다. 그러므로 원시자료형이 담기는 보관함의 크기 또한 고정하는 것이 합당하다.

원시타입은 값 자체에 대한 변경이 불가능하지만, 변수에 다른 데이터를 할당할 수는 있다.

참조타입은 변수에 값이 아닌 주소를 저장한다. 

이 찾아갈 수 있는 주소에 유동적으로 변할 수 있는 데이터가 담기게 되고, 호출시에는 이 주소로 해당 데이터를 불러올 수 있다.