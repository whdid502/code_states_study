내장 고차 함수

js에는 내장함수와 같이 내장 고차 함수도 포함되어 있다.

filter라는 내장고차함수는 배열의 요소중 특정 조건을 만족하는 요소들만 걸러내는 메소드이다.

array.filter는 불리언으로 리턴이 되는 함수를 인자로 받는다. 참인 배열의 요소만 담긴 배열을 리턴한다.

array.foreach는 배열을 순회하며 인자로 받은 콜백함수를 수행한다.

array.find는 콜백함수를 만족하는 첫번째 요소의 값을 반환한다. 그런 값이 없다면 undefined를 반환한다.

array.map은 배열내의 모든 요소 각각에 콜백함수를 호추출한 결과를 모아 새 배열을 반환한다.

array.reduce는 배열의 각 요소에 대해 주어진 리듀서 함수를 실행하고 하나의 결과값을 반환한다.

array.sort는 배열의요소를 적절한 위치에 정렬한 후 그 배열을 반환한다. stable sort가 아닐 수 있다.
정렬 속도와 복잡도는 구현방식에 따라 다르다.

array.some은 배열안의 어떤 요소라도 주어진 판별함수를 통과하는지 테스트한다. 하나라도 통과하면 true를 반환한다. 빈배열은 false를 반환한다.

array.every는 모든 배열의 요소가 콜백함수를 통과하는지 테스트한다. 하나라도 통과하지 못한다면 false를 반환한다. 빈배열을 호출하면 true를 반환한다.

